name: Ubuntu VPS + Ngrok للاتصال عن بعد

on:
  push:
    branches:
      - main

jobs:
  vps:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: تثبيت الحزم الأساسية
        run: |
          sudo apt update
          sudo apt install -y curl wget openssh-server unzip

      - name: إعداد وتشغيل SSH
        run: |
          # إنشاء مجلد SSH
          sudo mkdir -p /var/run/sshd

          # تغيير كلمة مرور root
          echo "root:Admin@123" | sudo chpasswd

          # السماح بتسجيل الدخول بكلمة المرور
          sudo sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config

          # إعادة تشغيل خدمة SSH
          sudo service ssh restart
          echo "✅ تم تشغيل SSH بنجاح"

      - name: إنشاء مجلد للملفات الصوتية
        run: |
          mkdir -p ~/quran-audio
          cd ~/quran-audio
          echo "📁 تم إنشاء مجلد quran-audio"

      - name: تحميل ملفات الصوت
        run: |
          cd ~/quran-audio
          BASE_URL="https://server12.mp3quran.net/maher/Almusshaf-Al-Mo-lim"
          
          echo "==========================================="
          echo "بدء تحميل الملفات المتوفرة..."
          echo "==========================================="
          
          # تحميل الملف الأول
          echo "📥 تحميل: 001.mp3"
          wget -q --show-progress "${BASE_URL}/001.mp3" -O "001.mp3"
          
          # تحميل الملفات من 078 إلى 114
          for i in $(seq 78 114); do
            if [ $i -lt 100 ]; then
              FILE_NUM="0${i}"
            else
              FILE_NUM="${i}"
            fi
            
            echo "📥 تحميل: ${FILE_NUM}.mp3"
            wget -q --show-progress "${BASE_URL}/${FILE_NUM}.mp3" -O "${FILE_NUM}.mp3"
            sleep 1
          done
          
          echo ""
          echo "✅ تم تحميل 38 ملف بنجاح"
          echo "📊 الحجم الإجمالي: $(du -sh ~/quran-audio/ | cut -f1)"

      - name: تثبيت وتشغيل ngrok
        run: |
          # تحميل وتثبيت ngrok
          curl -L https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.zip -o ngrok.zip
          unzip -o ngrok.zip
          sudo mv ngrok /usr/local/bin/ngrok
          
          # إضافة التوكن (هذا توكن تجريبي - يفضل استخدام توكنك الخاص)
          ngrok config add-authtoken 2gzCXiehyX6Dvdjyam6CeA3P7wN_4ZRSp9hHWtNdUrEMfW7A5
          
          echo "✅ تم تثبيت ngrok"

      - name: تشغيل أنفاق ngrok
        run: |
          # تشغيل نفق SSH (المنفذ 22)
          echo "🚇 تشغيل نفق SSH على المنفذ 22..."
          ngrok tcp 22 --log=stdout > ngrok-ssh.log &
          
          # تشغيل نفق HTTP للملفات (المنفذ 8080)
          echo "🚇 تشغيل نفق HTTP للملفات على المنفذ 8080..."
          cd ~/quran-audio/
          python3 -m http.server 8080 > /dev/null 2>&1 &
          ngrok http 8080 --log=stdout > ngrok-http.log &
          
          # انتظار تشغيل الأنفاق
          echo "⏳ جاري تشغيل الأنفاق..."
          sleep 20

      - name: الحصول على روابط ngrok
        run: |
          echo ""
          echo "==========================================="
          echo "🔥 VPS جاهز للاتصال عن بعد! 🔥"
          echo "==========================================="
          echo ""
          
          # الحصول على رابط SSH
          SSH_URL=$(curl -s http://127.0.0.1:4040/api/tunnels | grep -o 'tcp://[^"]*' | head -1)
          if [ ! -z "$SSH_URL" ]; then
            SSH_HOST=$(echo $SSH_URL | sed 's/tcp:\/\///' | cut -d: -f1)
            SSH_PORT=$(echo $SSH_URL | sed 's/tcp:\/\///' | cut -d: -f2)
            
            echo "🔹 اتصال SSH (من Termux أو أي جهاز):"
            echo "   الأمر: ssh root@$SSH_HOST -p $SSH_PORT"
            echo "   كلمة المرور: Admin@123"
            echo ""
            echo "   أو استخدم:"
            echo "   ssh root@$SSH_HOST -p $SSH_PORT"
            echo ""
          else
            echo "❌ فشل في الحصول على رابط SSH"
          fi
          
          # الحصول على رابط HTTP للملفات
          HTTP_URL=$(curl -s http://127.0.0.1:4041/api/tunnels 2>/dev/null | grep -o 'https://[^"]*' | head -1)
          if [ -z "$HTTP_URL" ]; then
            HTTP_URL=$(curl -s http://127.0.0.1:4040/api/tunnels | grep -o 'https://[^"]*' | head -1)
          fi
          
          if [ ! -z "$HTTP_URL" ]; then
            echo "🔹 رابط تحميل الملفات الصوتية:"
            echo "   $HTTP_URL"
            echo ""
            echo "   لتحميل ملف معين في Termux:"
            echo "   wget $HTTP_URL/001.mp3"
            echo "   wget $HTTP_URL/078.mp3"
            echo ""
            echo "   لتحميل كل الملفات:"
            echo "   for i in 001 078 079 080 081 082 083 084 085 086 087 088 089 090 091 092 093 094 095 096 097 098 099 100 101 102 103 104 105 106 107 108 109 110 111 112 113 114; do"
            echo "     wget $HTTP_URL/\${i}.mp3"
            echo "   done"
          else
            echo "❌ فشل في الحصول على رابط HTTP"
          fi
          
          echo ""
          echo "==========================================="
          echo "📁 الملفات المتوفرة في ~/quran-audio/:"
          cd ~/quran-audio/
          ls -la *.mp3 | head -10
          echo "... وعشرات الملفات الأخرى"
          echo "==========================================="
          echo ""

      - name: إنشاء سكربت مساعد للاتصال
        run: |
          cd ~
          
          # الحصول على روابط ngrok
          SSH_URL=$(curl -s http://127.0.0.1:4040/api/tunnels | grep -o 'tcp://[^"]*' | head -1)
          SSH_HOST=$(echo $SSH_URL | sed 's/tcp:\/\///' | cut -d: -f1)
          SSH_PORT=$(echo $SSH_URL | sed 's/tcp:\/\///' | cut -d: -f2)
          
          HTTP_URL=$(curl -s http://127.0.0.1:4040/api/tunnels | grep -o 'https://[^"]*' | head -1)
          
          # إنشاء ملف معلومات
          cat > connection_info.txt << EOF
===========================================
معلومات الاتصال بجهاز VPS
===========================================

📱 SSH (للتحكم الكامل):
   الأمر: ssh root@$SSH_HOST -p $SSH_PORT
   كلمة المرور: Admin@123

🌐 HTTP (لتحميل الملفات):
   الرابط: $HTTP_URL

📂 لتحميل جميع الملفات الصوتية في Termux:
   cd ~/storage/downloads
   mkdir quran
   cd quran
   
   # تحميل الملفات
   for i in 001 078 079 080 081 082 083 084 085 086 087 088 089 090 091 092 093 094 095 096 097 098 099 100 101 102 103 104 105 106 107 108 109 110 111 112 113 114; do
     wget $HTTP_URL/\${i}.mp3
   done

✅ تم تحميل 38 ملف بنجاح!

ملاحظة: هذا الرابط صالح لمدة 6 ساعات
===========================================
EOF

          echo "📄 تم إنشاء ملف connection_info.txt"
          cat connection_info.txt

      - name: إبقاء الجهاز نشطاً وعرض المعلومات
        run: |
          # الحصول على الروابط
          SSH_URL=$(curl -s http://127.0.0.1:4040/api/tunnels | grep -o 'tcp://[^"]*' | head -1)
          SSH_HOST=$(echo $SSH_URL | sed 's/tcp:\/\///' | cut -d: -f1)
          SSH_PORT=$(echo $SSH_URL | sed 's/tcp:\/\///' | cut -d: -f2)
          HTTP_URL=$(curl -s http://127.0.0.1:4040/api/tunnels | grep -o 'https://[^"]*' | head -1)
          
          START_TIME=$(date +%s)
          
          while true; do
            CURRENT_TIME=$(date +%s)
            ELAPSED=$((CURRENT_TIME - START_TIME))
            HOURS=$((ELAPSED / 3600))
            MINUTES=$(( (ELAPSED % 3600) / 60 ))
            SECONDS=$((ELAPSED % 60))
            REMAINING=$((360 - ELAPSED / 60))  # 6 ساعات = 360 دقيقة
            
            clear || true
            echo "==========================================="
            echo "🔥 VPS متاح للاتصال عن بعد 🔥"
            echo "==========================================="
            echo ""
            echo "⏱️  الوقت المنقضي: ${HOURS}h ${MINUTES}m ${SECONDS}s"
            echo "⏳ الوقت المتبقي: $((REMAINING / 60))h $((REMAINING % 60))m"
            echo ""
            echo "📱 اتصال SSH:"
            echo "   ssh root@$SSH_HOST -p $SSH_PORT"
            echo "   كلمة المرور: Admin@123"
            echo ""
            echo "🌐 رابط الملفات:"
            echo "   $HTTP_URL"
            echo ""
            echo "📊 حالة الملفات:"
            cd ~/quran-audio/ 2>/dev/null
            MP3_COUNT=$(ls -1 *.mp3 2>/dev/null | wc -l)
            TOTAL_SIZE=$(du -sh . 2>/dev/null | cut -f1)
            echo "   الملفات: $MP3_COUNT | الحجم: $TOTAL_SIZE"
            echo ""
            echo "📝 مثال تحميل في Termux:"
            echo "   wget $HTTP_URL/001.mp3"
            echo ""
            echo "🔄 سيتم التحديث كل 30 ثانية"
            echo "==========================================="
            
            sleep 30
          done
